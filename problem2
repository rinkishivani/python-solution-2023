
''' Problem 2:  Determine whether a given string is Palindrome
 A string “madam” is a palindromic string because it reads the same forwards and backward. 
Input: “anna”
Output: true

Input: “India”
Output: false
'''

# take user input
userinput = input("Enter a word to determine whether a given string is Palindrome or not:\n")

# reverse the word 
reversedword = userinput[::-1]

#check if word is Palindrome
if userinput == reversedword:
    print("TRUE\n",userinput, "is a palindrome\n i.e. ", userinput, " = ", reversedword)
else:
    
    print("FALSE\n",userinput, "is NOT a palindrome\n i.e. ", userinput, " != ", reversedword)


